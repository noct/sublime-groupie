sublime-groupie
===============

Simple Sublime Text 2/3 plugin for sorting files into layout groups

    # Example User Settings
    {
      "sort": true,
      "default_group": 0,
      "rules":
      [
        {
          "index": 1,
          "match": ["*.h", "*.hpp", "*.sublime-*"]
        }
      ]
    }
